# CircleCI CLI

Control whether CircleCI uses AI to summarize failed build errors for
this organization.

When enabled, CircleCI generates an AI summary of failure output
shown in the UI alongside the raw logs.

JSON fields: enable_ai_error_summarization

For more information about output formatting flags, see `circleci help formatting`.

## Usage

`circleci org settings ai-error-summarization [flags]`

## Flags

| Flag           | Description                                              |
| -------------- | -------------------------------------------------------- |
| `--disable`    | Disable the setting                                      |
| `--enable`     | Enable the setting                                       |
| `--jq string`  | Process values from the response using jq syntax         |
| `--json`       | Output as JSON                                           |
| `--org string` | Org slug or UUID (e.g. gh/myorg); defaults to git remote |

## Inherited Flags

| Flag                  | Description                                                  |
| --------------------- | ------------------------------------------------------------ |
| `-c, --config string` | Path to config file (default: ~/.config/circleci/config.yml) |
| `--debug`             | Enable debug logging                                         |
| `--no-color`          | Disable ANSI color output (same as setting NO_COLOR)         |
| `-q, --quiet`         | Suppress informational output. Data on stdout is unaffected  |

## Examples

- Show current value and pick a new one interactively (TTY): 
  `circleci org settings ai-error-summarization`
- Show current value for a specific org (non-interactive): 
  `circleci org settings ai-error-summarization --org gh/myorg`
- Enable the setting directly (non-interactive / scripting): 
  `circleci org settings ai-error-summarization --enable`
- Disable the setting directly (non-interactive / scripting): 
  `circleci org settings ai-error-summarization --disable`
- Output the current value as JSON: 
  `circleci org settings ai-error-summarization --json`

## Learn More

- Use `circleci <command> <subcommand> --help` for more information about a command.
- Read the manual at <https://cli.circleci.com/reference/>
- Support at <https://github.com/CircleCI-Public/circleci-cli/issues>

