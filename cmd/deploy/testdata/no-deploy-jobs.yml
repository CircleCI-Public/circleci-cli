version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run: echo "build"

  test:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run: make test
