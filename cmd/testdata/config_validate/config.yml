version: 2.1

orbs:
  node: circleci/node@5.0.3

jobs:
  hello-world:
    docker:
      - image: cimg/base:stable
    steps:
      - run: |
          echo "Hello world!"
workflows:
  datadog-hello-world:
    jobs:
      - hello-world
