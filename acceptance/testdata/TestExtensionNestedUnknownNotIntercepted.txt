Work with CircleCI runs.

Runs are the top-level unit of work in CircleCI — they contain
one or more workflows, each of which contains jobs.

Usage:
  circleci run [command]

Available Commands:
  cancel      Cancel a run
  get         Get a run's status
  list        List recent runs for a project
  open        Open the current project's runs page in the browser
  trigger     Trigger a new run
  watch       Watch a run until it completes

Flags:
  -h, --help   help for run

Global Flags:
  -c, --config string   path to config file (default: ~/.config/circleci/config.yml)
      --debug           enable debug logging
  -q, --quiet           suppress informational output; data on stdout is unaffected

Use "circleci run [command] --help" for more information about a command.
